# z80-CPM-board-with-smart-i-o
This is yet another retro computer running CPM on an actual Z80 CPU with a twist. Instead of using a traditional UART for communications, it uses a RPi pico module that is no bigger and no more expensive than a UART.
